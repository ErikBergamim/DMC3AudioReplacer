<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="GData.afs VAGp Manager" Height="680" Width="1040" Background="#FFF6F7FB">

    <Window.Resources>
        <!-- Theme brushes -->
        <SolidColorBrush x:Key="BrushBg" Color="#FFF6F7FB" />
        <SolidColorBrush x:Key="BrushSurface" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="BrushSurfaceAlt" Color="#FFF8FAFC" />
        <SolidColorBrush x:Key="BrushBorder" Color="#FFE2E8F0" />
        <SolidColorBrush x:Key="BrushText" Color="#FF0F172A" />
        <SolidColorBrush x:Key="BrushSubText" Color="#FF475569" />
        <SolidColorBrush x:Key="BrushMuted" Color="#FF64748B" />
        <SolidColorBrush x:Key="BrushPrimary" Color="#FF2563EB" />
        <SolidColorBrush x:Key="BrushPrimaryHover" Color="#FF1D4ED8" />
        <SolidColorBrush x:Key="BrushPrimaryDisabled" Color="#FF93C5FD" />

        <!-- Typography -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource BrushText}" />
            <Setter Property="TextWrapping" Value="NoWrap" />
        </Style>

        <!-- Button (primary-ish by default) -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BrushPrimary}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushPrimary}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,7" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BrushPrimaryHover}" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BrushPrimaryHover}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BrushPrimaryDisabled}" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BrushPrimaryDisabled}" />
                                <Setter Property="Foreground" Value="#FFF8FAFC" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- "Ghost" button for secondary actions (opt-in) -->
        <Style x:Key="GhostButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource BrushText}" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFF1F5F9" />
                    <Setter Property="BorderBrush" Value="#FFCBD5E1" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{StaticResource BrushMuted}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.75" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#FF93C5FD" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBox -->
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                            <ScrollViewer Focusable="False" Padding="{TemplateBinding Padding}">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="8">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FFF1F5F9" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FFDBEAFE" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListView -->
        <Style TargetType="ListView">
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListView">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ScrollViewer Focusable="False">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- GridView header -->
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#FFF8FAFC" />
            <Setter Property="Foreground" Value="{StaticResource BrushSubText}" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <!-- Make inner action buttons look secondary -->
        <Style x:Key="TableActionButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
            <Setter Property="MinWidth" Value="90" />
            <Setter Property="Padding" Value="10,6" />
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header / Folder bar -->
        <Border Grid.Row="0" CornerRadius="12" Background="{StaticResource BrushSurface}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Padding="14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="GData.afs" FontSize="22" FontWeight="SemiBold" Foreground="{StaticResource BrushText}" />
                    <TextBlock Text="Select folder and manage VAGp blocks by offset" Margin="0,2,0,10" Foreground="{StaticResource BrushSubText}" />

                    <DockPanel LastChildFill="True">
                        <Button x:Name="BrowseButton" Content="Browse folder..." MinWidth="140" Height="34" Margin="0,0,10,0" Click="BrowseButton_Click" Style="{StaticResource GhostButton}" />
                        <TextBox x:Name="FolderTextBox" Height="34" />
                    </DockPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button x:Name="ScanButton" Content="Load" MinWidth="110" Height="34" Margin="0,0,10,0" Click="ScanButton_Click" />
                    <Button x:Name="BulkExportButton" Content="Exportar vários" MinWidth="140" Height="34" Margin="0,0,10,0" Click="BulkExportButton_Click" Style="{StaticResource GhostButton}" />
                    <Button x:Name="BulkReplaceButton" Content="Substituir vários" MinWidth="150" Height="34" Margin="0,0,10,0" Click="BulkReplaceButton_Click" Style="{StaticResource GhostButton}" />
                    <Button x:Name="SaveButton" Content="Salvar JSON" MinWidth="120" Height="34" IsEnabled="False" Click="SaveButton_Click" Style="{StaticResource GhostButton}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main content -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Files panel -->
            <Border CornerRadius="12" Background="{StaticResource BrushSurface}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Padding="12">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Arquivos" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>

                    <TextBox x:Name="FileFilterTextBox" DockPanel.Dock="Top" Height="30" Margin="0,0,0,10"
                             TextChanged="FileFilterTextBox_TextChanged"
                             ToolTip="Filtrar por nome" />

                    <ListBox x:Name="FilesListBox" SelectionChanged="FilesListBox_SelectionChanged" />
                </DockPanel>
            </Border>

            <!-- VAGp panel -->
            <Border Grid.Column="2" CornerRadius="12" Background="{StaticResource BrushSurface}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0">
                        <TextBlock Text="Blocos VAGp" FontSize="16" FontWeight="SemiBold" />
                        <TextBlock x:Name="SelectedFileText" Margin="10,2,0,0" Foreground="{StaticResource BrushMuted}" />
                    </DockPanel>

                    <TextBlock Grid.Row="1" x:Name="HintText" Margin="0,6,0,10" Foreground="{StaticResource BrushSubText}"
                               Text="Selecione um arquivo à esquerda para ver os VAGp encontrados."/>

                    <ListView Grid.Row="2" x:Name="VagpListView" Margin="0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="#" Width="70" DisplayMemberBinding="{Binding Name}"/>
                                <GridViewColumn Header="Offset" Width="120" DisplayMemberBinding="{Binding Offset}"/>
                                <GridViewColumn Header="Length" Width="120" DisplayMemberBinding="{Binding Length}"/>
                                <GridViewColumn Header="SampleRate" Width="120" DisplayMemberBinding="{Binding SampleRate}"/>
                                <GridViewColumn Header="Ações" Width="340">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="Tocar som" Margin="0,0,8,0" Tag="{Binding}" Click="PlaySoundButton_Click" Style="{StaticResource TableActionButton}"/>
                                                <Button Content="Exportar" Margin="0,0,8,0" Tag="{Binding}" Click="ExportButton_Click" Style="{StaticResource TableActionButton}"/>
                                                <Button Content="Substituir..." Tag="{Binding}" Click="ReplaceButton_Click" Style="{StaticResource TableActionButton}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <!-- WAV player -->
                    <Border Grid.Row="3" Margin="0,12,0,0" CornerRadius="10" Background="{StaticResource BrushSurfaceAlt}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Button x:Name="PlayerPlayButton" Content="Play" Width="70" Margin="0,0,10,0" Click="PlayerPlayButton_Click" IsEnabled="False" Style="{StaticResource GhostButton}" />
                            <Button x:Name="PlayerStopButton" Grid.Column="1" Content="Stop" Width="70" Margin="0,0,10,0" Click="PlayerStopButton_Click" IsEnabled="False" Style="{StaticResource GhostButton}" />
                            <Slider x:Name="PlayerSlider" Grid.Column="2" Minimum="0" Maximum="1" Value="0" IsEnabled="False" ValueChanged="PlayerSlider_ValueChanged" Margin="0,2" />
                            <TextBlock x:Name="PlayerTimeText" Grid.Column="3" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="{StaticResource BrushSubText}" Text="00:00 / 00:00" />
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="3" CornerRadius="10" Background="{StaticResource BrushSurface}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Padding="10" Margin="0,12,0,0">
            <DockPanel>
                <TextBlock x:Name="StatusText" Foreground="{StaticResource BrushText}" />
                <TextBlock DockPanel.Dock="Right" x:Name="CountsText" Foreground="{StaticResource BrushMuted}" />
            </DockPanel>
        </Border>
    </Grid>
</Window>
